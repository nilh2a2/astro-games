---
import { type CollectionEntry, render } from "astro:content";
import Layout from "@/layouts/Layout.astro";
import Header from "@/components/Header.astro";
import Footer from "@/components/Footer.astro";
import BackButton from "@/components/BackButton.astro";
import BackToTopButton from "@/components/BackToTopButton.astro";
import GameMetadata from "@/components/GameMetadata.astro";
import RelatedGames from "@/components/RelatedGames.astro";
import GameCardCompact from "@/components/GameCardCompact.astro";
import ShareLinks from "@/components/ShareLinks.astro";
import { getCategoryPath } from "@/utils/getGamePath";
import getRelatedGames from "@/utils/getRelatedGames";
import { slugifyStr } from "@/utils/slugify";
import { getUiText } from "@/utils/getUiText";
import { SITE } from "@/config";

type Props = {
  game: CollectionEntry<"games">;
  games: CollectionEntry<"games">[];
};

const { game, games } = Astro.props;

const {
  title,
  description,
  gameUrl,
  coverImage,
  thumbnail,
  ogImage: initOgImage,
  canonicalURL,
  pubDatetime,
  modDatetime,
  category,
} = game.data;

const { Content } = await render(game);

const coverImg = coverImage || thumbnail;

let ogImageUrl: string | undefined;

if (typeof initOgImage === "string") {
  ogImageUrl = initOgImage;
} else if (initOgImage?.src) {
  ogImageUrl = initOgImage.src;
}

const ogImage = ogImageUrl
  ? new URL(ogImageUrl, Astro.url.origin).href
  : undefined;

const layoutProps = {
  title: `${title} | ${SITE.title}`,
  description,
  pubDatetime,
  modDatetime,
  canonicalURL,
  ogImage,
  scrollSmooth: true,
};

const relatedGamesList = getRelatedGames(games, game, 18);
const sidebarGames = relatedGamesList.slice(0, 5);
const bottomGames = relatedGamesList.slice(5);
---

<Layout {...layoutProps}>
  <Header />
  <BackButton />
  <main id="main-content" class="app-layout pb-12 mt-8">
    <!-- Breadcrumb -->
    <nav class="mb-6 flex items-center gap-2 text-sm">
      <a href="/" class="text-skin-accent hover:underline">{getUiText("breadcrumb.home")}</a>
      <span class="text-skin-base/60">{getUiText("breadcrumb.separator")}</span>
      <a href={getCategoryPath(category)} class="text-skin-accent hover:underline capitalize">
        {category}
      </a>
      <span class="text-skin-base/60">{getUiText("breadcrumb.separator")}</span>
      <span class="text-skin-base">{title}</span>
    </nav>

    <!-- Game Header -->
    <div class="mb-8">
      <h1
        transition:name={slugifyStr(title.replaceAll(".", "-"))}
        class="mb-4 text-3xl font-bold text-accent sm:text-4xl"
      >
        {title}
      </h1>
      <GameMetadata game={game} />
    </div>

    <!-- Game Player Section with Related Games Sidebar -->
    <div class="mb-12 flex flex-col lg:flex-row gap-6">
      <!-- Left Column: Game Player (70%) -->
      <div class="flex-1 lg:w-7/10">
        <div class="relative aspect-video overflow-hidden rounded bg-black">
          <iframe
            src={gameUrl}
            title={title}
            class="h-full w-full border-0"
            allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
            allowfullscreen
          />
        </div>
      </div>

      <!-- Right Column: Related Games Sidebar (30%) - Desktop Only -->
      <div class="hidden lg:flex flex-shrink-0 lg:w-3/10">
        <div class="w-full">
          <div class="max-h-[600px] overflow-y-auto pr-2 space-y-3 scrollbar-thin scrollbar-thumb-skin-accent/30 scrollbar-track-transparent">
            {sidebarGames.map(relatedGame => (
              <GameCardCompact {...relatedGame} />
            ))}
          </div>
        </div>
      </div>
    </div>

    <!-- Share Links -->
    <div class="mb-8">
      <ShareLinks />
    </div>

    <!-- Game Content -->
    <div class="mb-12 app-prose max-w-none">
      <Content />
    </div>

    <hr class="my-8 border-dashed" />

    <!-- Related Games -->
    {relatedGamesList.length > 0 && (
      <div class="mb-12">
        <h2 class="mb-6 text-2xl font-bold">{getUiText("game.relatedGames")}</h2>
        <!-- Mobile: Show all games (sidebar hidden on mobile) -->
        <div class="lg:hidden">
          <RelatedGames games={relatedGamesList} />
        </div>
        <!-- Desktop: Show remaining games (first 5 are in sidebar) -->
        <div class="hidden lg:block">
          {bottomGames.length > 0 && <RelatedGames games={bottomGames} />}
        </div>
      </div>
    )}

    <BackToTopButton />
  </main>
  <Footer />
</Layout>
